<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Site Settings</title>
  <link href="/css/bootstrap.min.css" rel="stylesheet"/>
  <link href="/icons/bootstrap-icons.css" rel="stylesheet"/>
  <style>
    body { background: #f0f2f5; }
    .card { border: none; border-top: 4px solid #667eea; border-radius: .75rem; box-shadow: 0 .25rem .75rem rgba(0,0,0,0.1);}  
    .form-control { border-radius: .5rem; }
    .actions .btn { min-width: 120px; }
  </style>
</head>
<body>
  <!-- Navbar -->
  <%- include('partials/header', { siteSettings: { name: site_name, description: site_description } }) %>

  <main class="container py-5">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="card p-4">
          <h3 class="card-title text-center mb-4">Site Settings</h3>
          <form action="/organiser/settings" method="POST">
            <div class="mb-3">
              <label for="site_name" class="form-label">Site Name</label>
              <input type="text" id="site_name" name="site_name" value="<%= site_name %>" class="form-control" required />
            </div>
            <div class="mb-3">
              <label for="site_description" class="form-label">Site Description</label>
              <textarea id="site_description" name="site_description" rows="4" class="form-control" required><%= site_description %></textarea>
            </div>
            <div class="text-center actions">
              <button type="submit" class="btn btn-primary me-2">
                <i class="bi bi-save me-1"></i>Save
              </button>
              <a href="/organiser" class="btn btn-outline-secondary">
                <i class="bi bi-x-circle me-1"></i>Cancel
              </a>
            </div>
          </form>
        </div>
      </div>
    </div>
  </main>

  <script src="/js/bootstrap.bundle.min.js"></script>
</body>
</html>
